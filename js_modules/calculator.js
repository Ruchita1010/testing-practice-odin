const calculator = {
  add: (a, b) => {
    return a + b;
  },

  subtract: (a, b) => {
    return b - a;
  },

  multiply: (a, b) => {
    return a * b;
  },

  divide: (a, b) => {
    return b / a;
  },
};

export default calculator;
